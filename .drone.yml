kind: pipeline
name: vue_bolg
type: exec
steps:
  - name: pull
    commands:
      - sudo rsync -a ./ /www/wwwroot/vue_blog
      - sudo chmod a+x /www/wwwroot/vue_blog
    when:
      branch:
        - master
